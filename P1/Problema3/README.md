# Librerias (en caso de usar anaconda ya vienen, sino, se deben instalar)
pip install pandas 
pip install numpy
pip install matplotlib

# Librería a instalar
pip install seaborn
ipywidgets	pip install ipywidgets	